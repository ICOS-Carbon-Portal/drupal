<?php

/**
* Implements hook_mail().
*/
function cp_email_mail($key, &$message, $params) {
	var_dump($params);
	$message['from'] = \Drupal::config('system.site')->get('mail');
	$message['subject'] = $params['subject'];
	$message['body'][] = $params['body'];
}

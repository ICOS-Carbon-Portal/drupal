(function ($) {
  'use strict';
  Drupal.behaviors.cp_blogs = {
    attach: function(context, settings) {
      
    	
    	
        $(window).resize(function () {
        	
        });
                
    }
  };
}(jQuery));
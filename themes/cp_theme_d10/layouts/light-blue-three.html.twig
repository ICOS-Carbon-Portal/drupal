{#
/**
 * @file
 * Theme implementation to display a full page width, three-column layout with a light blue background.
 *
 * Available variables:
 * - in_preview: Whether the plugin is being rendered in preview mode.
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout <div>.
 *
 * @ingroup themeable
 */
#}

{%
  set classes = [
    'layout',
    'layout--light-blue',
    'pt-' ~ (settings.spacing_top ?? 5),
    'pb-' ~ (settings.spacing_bottom ?? 5),
  ]
%}

{%
  set one_classes = [
    'layout__region',
    'layout__region--one',
    'col-lg'
  ]
%}

{%
  set two_classes = [
    'layout__region',
    'layout__region--two',
    'col-lg'
  ]
%}

{%
  set three_classes = [
    'layout__region',
    'layout__region--three',
    'col-lg'
  ]
%}

{% if content %}
  <div{{ attributes.addClass(classes) }}>
    <div class="row justify-content-between gap-3">
      {% if content.one %}
        <div{{ region_attributes.one.addClass(one_classes) }}>
          {{ content.one }}
        </div>
      {% endif %}
      {% if content.two %}
        <div{{ region_attributes.two.addClass(two_classes) }}>
          {{ content.two }}
        </div>
      {% endif %}
      {% if content.three %}
        <div{{ region_attributes.three.addClass(three_classes) }}>
          {{ content.three }}
        </div>
      {% endif %}
    </div>
  </div>
{% endif %}
